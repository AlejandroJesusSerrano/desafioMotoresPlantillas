include layout.pug

h2 Lista de Productos de Asgard Coleccionables

if products.length === 0
    div(class="container")
        h3 No hay productos en la coleccion
else
    table(class="table table-dark")
        thead
            tr
                th Producto
                th Precio
                th Universe
                th Descripcion
                th Cant. Disponible
                th Imagen
        tbody(class="container")
            each product in products
                tr
                    td{product.title}
                    td{product.price}
                    td{product.universe}
                    td{product.description}
                    td{product.stock}
                    td
                        img(src="/img/{product.image}")
            
            
a (href="./products")
    button Cargar Productos



